@model IEnumerable<WebApplicationSistemaPesquisaFinal.Models.ViewPopuli>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.CHAVE)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.NOME_PESSOA)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.EMAIL)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.SIGLA)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.CHAVE)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.NOME_PESSOA)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.EMAIL)
        </td>
        <td>
            
            @Html.DisplayFor(modelItem => item.SIGLA)
            <input type="checkbox" name="SIGLA" value="" />
        </td>
        <td>
            @Html.ActionLink("Editar", "Edit", new { id=item.ID_PESSOA }) |
            @Html.ActionLink("Detalhes", "Details", new { id=item.ID_PESSOA }) |
            @Html.ActionLink("Excluir", "Delete", new { id=item.ID_PESSOA })
        </td>
    </tr>
}

</table>
